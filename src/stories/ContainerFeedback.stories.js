import React from "react";
import { ContainerFeedback } from "investira.react.components";

export default {
  title: "ContainerFeedback",
  component: ContainerFeedback,
};

export const Default = () => {
  return <ContainerFeedback> Clique em qualquer lugar! </ContainerFeedback>;
};
